# Migrations

EF Core migrations for `EasyAgree.Infrastructure` (`backend/src/EasyAgree.Infrastructure/Persistence`)
are generated here via `dotnet ef migrations add <Name> -p backend/src/EasyAgree.Infrastructure -s backend/src/EasyAgree.Api`.
