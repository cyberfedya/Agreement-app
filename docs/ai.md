# AI

Prompt library in `ai/prompts/`, JSON schemas for structured outputs in `ai/schemas/`,
worked examples in `ai/examples/`, and evaluation sets in `ai/evaluations/`.

Pipeline: classify → extraction → questionnaire (missing fields) → agreement_generation → risk_analysis → negotiation.
