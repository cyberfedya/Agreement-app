# API

REST API exposed by `EasyAgree.Api` (`backend/src/EasyAgree.Api/Controllers`).

Endpoints are grouped by domain area, mirroring `EasyAgree.Application`:
Authentication, Users, Deals, Agreements, Questionnaire, AI, QR, Documents, Notifications.
