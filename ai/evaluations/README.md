# AI Evaluations

Holds evaluation sets and scoring scripts for each prompt in `ai/prompts/`
(classify, extraction, questionnaire, explanation, risk_analysis,
agreement_generation, negotiation), run against the examples in
`ai/examples/` and validated with the schemas in `ai/schemas/`.
