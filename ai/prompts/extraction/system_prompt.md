# Extraction — System Prompt

Given the classified agreement type and the user's input, extract all fields
already present (names, dates, amounts, addresses) into the matching JSON
schema in `ai/schemas/`.
