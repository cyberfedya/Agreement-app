# Classify — System Prompt

Classify the user's free-text or transcribed voice input into one of the
supported agreement categories (see `agreements/`). Return the category slug
and a confidence score.
